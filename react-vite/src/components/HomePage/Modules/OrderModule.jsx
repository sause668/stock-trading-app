/**
 * ### Order Module
 * Intended only for use within the `ProfilePage` component.
 * 
 * Displays relevant user data pertaining to orders they have created.
 * @param user The user data passed in from the parent.
 */
export default function OrderModule({ user }) {
    return (<div id="profile-left__orders">
        {/* Code here */}


    </div>)
}